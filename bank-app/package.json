{
  "name": "bank-of-molten",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bank of Molten — monorepo dev runner",
  "scripts": {
    "dev":         "node scripts/dev.js",
    "dev:client":  "cd client && PORT=3000 BROWSER=none npm start",
    "dev:server":  "cd server && npm run dev",
    "dev:admin":   "cd admin  && npm run dev",
    "install:all": "npm install && npm install --prefix client && npm install --prefix server && npm install --prefix admin",
    "build":       "npm run build --prefix client && npm run build --prefix admin",
    "health":      "curl -s http://localhost:8000/api/health | python3 -m json.tool"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
